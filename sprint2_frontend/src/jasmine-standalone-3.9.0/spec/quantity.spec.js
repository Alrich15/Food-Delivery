describe('Quantity reducer caller', QuantityReducer(){
    beforeEach(QuantityReducer(){
        var throw;
    });


    it("should return 2 when INCREMENT called", function(){
        throw = 'INCREMENT';
        expect(throw).toEqual(2);
    });
// describe("QuantityReducer", ->
//     counter = new QuantityReducer
//     it("should have 1 as a count variable at first", ->
//         expect(counter.count).toBe(1)
//     )

//     describe('increment()', ->
//         it("should count up from 0 to 1", ->
//             expect(counter.increment()).toBe(1)
//         )
}